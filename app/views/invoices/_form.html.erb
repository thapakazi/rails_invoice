<%= simple_form_for @invoice  do |form| %>

<% if @invoice.errors.any? %>
<div id="error_explanation">
  <h2>
    <%= pluralize(@invoice.errors.count, "error") %> prohibited
    this invoice from being saved:
  </h2>
  <ul>
    <% @invoice.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>
<p>
  <%= form.input :item, label: "Item"%>
</p>

<p>
  <%= form.input :details, label: "Details"%>
</p>

<p>
  <%= form.input :price, as: :currency, label: "Cost" %>
</p>

<p>
  <%= form.button :submit %>
</p>
<% end %>
