
<h1>Listing invoices</h1>
<table>
  <tr>
    <th>Item</th>
    <th>Detail</th>
    <th>Price</th>
    <th>Show</th>
    <th>Edit</th>

  </tr>
 
  <% @invoices.each do |invoice| %>
  
    <tr>
      <td><%= invoice.item %></td>
      <td><%= invoice.details %></td>
      <td><%= invoice.price %></td>
      <td><%= link_to '👓', invoice_path(invoice) %></td>
      <td><%= link_to '🖊', edit_invoice_path(invoice) %></td>
    </tr>
  <% end %>
</table>
 
<br/>
<%= link_to 'New invoice', new_invoice_path %>
